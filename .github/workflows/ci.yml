name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: fmt, clippy, build, test
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Toolchain (rust-toolchain.toml)
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.98.0
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Format
        run: cargo fmt --all -- --check
      - name: Clippy
        run: cargo clippy --workspace --all-targets --locked
      - name: Build
        run: cargo build --workspace --release --locked
      - name: Test
        # `real_runtime_*` testi duvar saatine bağlıdır (6 blok ~60 sn ister);
        # paylaşımlı CI koşucusunda 2 vCPU ile blok üretimi yetişmiyor ve test
        # yanlış negatif veriyor. Bu test yerelde tam koşuda (cargo test --workspace)
        # ve her dağıtım öncesi çalıştırılır; CI'da atlanır.
        run: cargo test --workspace --locked --no-fail-fast -- --skip real_runtime_
